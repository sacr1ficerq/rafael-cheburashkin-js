/*
Exercise 20: Calculator
Use the operator to calculate with two numbers.
Supported operators: +, -, *, /
Return null when the operator is not supported.
*/

function calculator(a, operator, b) {
  return undefined;
}

module.exports = { calculator };
